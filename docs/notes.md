- PyMuPDF get_text() может возвращать слова в неправильном порядке.
- Для таблиц использовать get_text("words") и сортировку по (y,x).
- Для тестовых PDF с кириллицей нужен DejaVuSans.ttf.
- Parser лучше строить как набор extractors по полям.